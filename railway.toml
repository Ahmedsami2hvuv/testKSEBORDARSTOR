[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && NODE_OPTIONS=--max-old-space-size=4096 next build"

[deploy]
startCommand = "node scripts/start-production.cjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
